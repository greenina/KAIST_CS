import React, { useRef } from 'react'
import './style.css'

const Intro = (props)=>{
    return(
        <div id="intro" style={{height:500}}>
            <h1>전산학부 학생회는 말이죠~~~~</h1>
            짱이에요!!
        </div>
    )
}

export default Intro;